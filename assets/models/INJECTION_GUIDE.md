# AI Target Injection Zone
Drop your compiled models here:
1. `bio_clock_edge.ptl` (18.83 MB)
2. `bio_clock_item_mapping.json`

The Flutter engine will bundle them for offline Arrhenius execution on the PyTorch Lite Engine.
