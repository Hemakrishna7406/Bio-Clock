name: bio_clock_app
description: "Bio-Clock Offline Edge AI Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Phase 3 Inject: Hardware & ML dependencies
  flutter_blue_plus: ^1.16.5
  camera: ^0.10.5
  pytorch_lite: ^3.0.1
  path_provider: ^2.1.2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # PHASE 3 INJECTION ZONE FOR AI
  # Drop bio_clock_edge.ptl and bio_clock_item_mapping.json here manually
  assets:
    - assets/models/
    - assets/data/
